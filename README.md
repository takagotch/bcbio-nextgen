### bcbio-nextgen
---
https://github.com/bcbio/bcbio-nextgen

```
bcbio_nextgetn.py upgrade --cwl

export TARGETDIR=~/install/bcbio-vm/anaconda
export BINDIR=/ur/local/bin
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh -b -p $TARGETDIR
TARGETDIR/bin/conda install --yes -c conda-forge -c bioconda python=3 bcbio-nextgen
TARGETDIR/bin/conda install --yes -c conda-forge -c bioconda python=3 bcbio-nextget-vm
mkdir -p $BINDIR
ls -s $TARGET/binbcbio_vm.py $BINDIR/bcbio_vm.py
ls -s $TARGETDIR/bin/conda $BINDIR/bcbiovm_conda
ls -s $TARGETIDR/bin/python $BINDIR/bcbiovm_python

```

```
```

```
```


